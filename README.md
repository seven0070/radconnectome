# RadConnectome

> *The agent with a wiring diagram. Every tool a synapse. Every agent a
> neuron. Every goal a behavior. Every claim a proof.*

Harness-first, graph-native, verification-first. Any brain, one wiring
diagram, machine proof for every claim. Spec: `manual/README.md`.

## P0 (v0.1)

- `rcx/connectome.py` — fruit fly wiring (neurons/synapses/STDP/rich-club)
- `rcx/tribe.py` — multimodal encoding prior (text/vision/audio → regions)
- `rcx/policy.py` — capabilities + hard shell/path/URL layer
- `rcx/verify.py` — check kinds + VERIFIED/FAILED/UNVERIFIED
- `rcx/arc.py` — ARC-style novel-task battery (novel by construction)

```bash
pip install -e .
python -m pytest -q
```

## Laws

VERIFIED-only · 16/60 budgets · Needle OFF · loopback Bearer ·
`~/.rcx` human-readable truth · observed, never invented.
